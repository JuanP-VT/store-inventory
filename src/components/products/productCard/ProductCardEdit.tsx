import React from "react";
type Props = {
  setEditMode: React.Dispatch<React.SetStateAction<boolean>>;
};

function ProductCardEdit({ setEditMode }: Props) {
  return <div>ProductCardEdit</div>;
}

export default ProductCardEdit;
